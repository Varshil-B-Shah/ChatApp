<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Base.Theme.ChatApp" parent="Theme.Material3.DayNight.NoActionBar" />

    <style name="Theme.ChatApp" parent="Base.Theme.ChatApp" />

    <style name="Theme.ChatApp.WithActionBar" parent="Theme.Material3.DayNight">
        <item name="windowActionBar">true</item>
        <item name="windowNoTitle">false</item>
        <item name="colorPrimary">@color/purple_primary</item>
        <item name="actionBarStyle">@style/ChatActionBar</item>
    </style>

    <style name="ChatActionBar" parent="Widget.AppCompat.ActionBar.Solid">
        <item name="titleTextStyle">@style/ChatActionBar.TitleText</item>
        <item name="background">@color/purple_primary</item>
    </style>

    <style name="ChatActionBar.TitleText" parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">@android:color/white</item>
    </style>

</resources>